apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dx-creds-{{ .Release.Name }}
  labels:
    {{- include  "clinvar-raw.chartLabels" . | nindent 4 }}
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcpsecretsmanager-secretstore
  target:
    name: dx-creds-{{ .Release.Name }} # K8S ExternalSecret resource name
  dataFrom:
  - extract:
      key: dx-creds # Google Secret Manager secret name



  # backendType: gcpSecretsManager
  # projectId: {{ .Values.project_id }}
  # data:
  #   - key: dx-creds
  #     property: kafka_user
  #     name: kafka_user
  #     version: latest
  #   - key: dx-creds
  #     property: kafka_password
  #     name: kafka_password
  #     version: latest
