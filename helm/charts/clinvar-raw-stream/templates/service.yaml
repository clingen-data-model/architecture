apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-svc"
  labels:
    {{- include  "clinvar-raw.chartLabels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "clinvar-raw.selectorLabels" . | nindent 4 }}
