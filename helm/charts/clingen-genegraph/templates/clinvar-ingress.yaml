{{- if .Values.clinvar_genegraph_deployment }}
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: genegraph-clinvar-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: {{ .Values.clinvar_static_ip_name }}
    networking.gke.io/managed-certificates: {{ .Values.genegraph_mcrt_name }}
    kubernetes.io/ingress.class: "gce"
spec:
  backend:
    serviceName: {{ .Values.clinvar_service_name }}
    servicePort: 8888
{{- end }}
